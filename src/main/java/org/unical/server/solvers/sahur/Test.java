package org.unical.server.solvers.sahur;

public class Test {
    public static void main(String[] args) {
        Solver solver = new Solver();
        System.out.println(solver.solve(null));
    }
}
